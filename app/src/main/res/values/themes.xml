<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- This is only used for the Activity window; Compose draws the UI. -->
    <style name="Theme.GramaSanjeevini" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">?android:colorBackground</item>
        <item name="android:navigationBarColor" tools:targetApi="l">?android:colorBackground</item>
    </style>

</resources>

